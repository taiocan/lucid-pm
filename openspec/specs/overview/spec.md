# System Overview

> Generated by openlore v1.0.0 on 2026-06-07 07:57
> Confidence: 90%

## Purpose

OpenSpec is a set of Rust libraries and tools designed to structure project management data. It uses
user-defined schemas and AI models to parse unstructured text (like meeting notes or tickets) into
validated, structured items, and provides suggestions for evolving the data ontology.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Project | Extracts structured items from unstructured text using a project schema and an external AI model (Gemini). | [spec.md](../project/spec.md) |
| Ontology | Provides AI-driven suggestions for ontology modifications based on a data snapshot and vocabulary constraints by querying an external generative AI service. | [spec.md](../ontology/spec.md) |

## Technical Stack

- **Type**: Library/Package
- **Primary Language**: Rust
- **Key Frameworks**: None detected
- **Architecture**: Event-Driven Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Adopted Rust for performance, memory safety, and building native command-line tooling.
- Utilized an event-driven architecture to model project history and state changes immutably.
- Relied on external Large Language Models (LLMs) for the core logic of parsing unstructured text, separating the system's orchestration logic from the AI's natural language capabilities.
- Employed a schema-first approach, where user-defined schemas are the source of truth for data structure and validation.
- Designed as a collection of loosely-coupled modules (libraries) rather than a single monolithic application, promoting reusability and separation of concerns.

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: Unstructured text and a project schema are provided to the ItemExtractorService. The service calls an external AI model to parse the text, producing structured ExtractedItem data. This data is likely transformed into a series of events which are persisted to a file-based log. Other components, like the OntologySuggester or reporting tools, read this event stream to build their state and perform their functions.

## Technical Notes

- **Architecture Style**: Event-driven, modular architecture. The system is composed of distinct libraries (modules) that operate on a stream of events representing changes to project data. This decouples components and allows for flexible processing and state reconstruction, suitable for a system managing evolving project information.
- **Security Model**: Not applicable. The analysis indicates the system is a set of local libraries and command-line tools. There is no evidence of network-exposed APIs, user accounts, or an authentication/authorization model.
- **External Integrations**: Google Gemini, Generative AI Service, File System
